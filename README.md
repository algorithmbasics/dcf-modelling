# dcf-modelling
Basic DCF Modelling

``eval "$(ssh-agent -s)" && ssh-add ~/.ssh/dcf-modelling`` \
``git config user.email "algorithmbasics@gmail.com" && git config user.name "Algorithm Basics"`` \
``git add . && git commit -m "added some files" && git push``

``git pull``